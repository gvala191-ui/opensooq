# 🚀 Быстрый старт - Деплой бота на сервер

## 🎯 Три варианта (от простого к сложному)

---

## 🆓 Вариант 1: Render.com - САМЫЙ ПРОСТОЙ (5 минут)

**Полностью бесплатно, без кредитки!**

### Шаги:
1. **Загрузи проект на GitHub** (создай репозиторий)
2. **Иди на [render.com](https://render.com)** → Регистрация через GitHub
3. **New +** → **Web Service**
4. **Подключи свой репозиторий**
5. **Настрой:**
   - Runtime: `Python 3`
   - Build: `pip install -r requirements.txt && playwright install chromium`
   - Start: `python bot.py`
6. **Добавь переменные:**
   - `BOT_TOKEN` = `8392139446:AAFKa4foNUq7vU1atOJtRgLERtW-Z0_o3Vc`
   - `ADMIN_ID` = `8373464271`
7. **Create Web Service**

✅ Готово! Бот работает 24/7

---

## 🐳 Вариант 2: Docker (если есть VPS)

```bash
# Подключись к серверу
ssh root@твой_сервер_ip

# Клонируй проект
git clone <твой_репозиторий>
cd opensoq

# Запусти через Docker Compose
docker-compose up -d

# Проверь логи
docker-compose logs -f
```

✅ Бот работает в контейнере!

---

## 💻 Вариант 3: VPS с автоматической установкой

```bash
# 1. Подключись к серверу
ssh root@твой_сервер_ip

# 2. Клонируй проект
git clone <твой_репозиторий>
cd opensoq

# 3. Настрой переменные
export BOT_TOKEN="8392139446:AAFKa4foNUq7vU1atOJtRgLERtW-Z0_o3Vc"
export ADMIN_ID="8373464271"

# 4. Запусти скрипт установки
chmod +x deploy.sh
./deploy.sh
```

✅ Бот установлен как системный сервис!

### Управление:
```bash
# Проверка статуса
systemctl status parser-bot

# Логи в реальном времени
journalctl -u parser-bot -f

# Перезапуск
systemctl restart parser-bot

# Остановка
systemctl stop parser-bot
```

---

## 🔑 Где взять VPS (от дешёвого к дорогому)

1. **Hetzner** - €3.79/мес (🔥 лучшая цена/качество)
2. **DigitalOcean** - $4/мес (дают $200 бонусов новичкам)
3. **Vultr** - $2.50/мес (самый дешёвый)
4. **Linode** - $5/мес
5. **AWS/Google Cloud** - $5-10/мес (есть бесплатный год)

---

## 📊 Что получишь после деплоя

✅ Бот работает **24/7** без твоего участия  
✅ Не зависит от **твоего ПК** и интернета  
✅ Все запросы идут **напрямую с сервера**  
✅ Управление через **Telegram из любой точки мира**  
✅ Автоматический **перезапуск** при сбоях  

---

## 🆘 Если что-то пошло не так

### Render.com не запускается?
- Проверь переменные окружения (BOT_TOKEN, ADMIN_ID)
- Посмотри логи во вкладке "Logs"

### VPS: "Permission denied"?
```bash
sudo chmod +x deploy.sh
sudo ./deploy.sh
```

### Docker: порт занят?
```bash
docker-compose down
docker-compose up -d
```

---

## 💡 Рекомендация

Для **начала** → Render.com (бесплатно, просто)  
Когда **нужна скорость** → VPS (полный контроль)

**После деплоя:** Открой Telegram → найди своего бота → `/start` → `/run` 🚀

---

📖 Подробная инструкция: [DEPLOY.md](DEPLOY.md)
